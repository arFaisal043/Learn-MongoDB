# MongoDB:

1. # Comparison query operator: $eq, $lt, $lte, $gt, $gte, $ne, $in, $nin
2. # Projection
3. # Logical operators: $and, $or, $not, and $nor.
4. # Sort, limit, distinct
5. # Aggregation:
   - Group -> group by in sql
   - Aggregation pipeline
6. # Evaluation Query Operators
   - $regex() 